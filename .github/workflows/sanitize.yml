name: public-release-checks

on:
  push:
  pull_request:

jobs:
  release-hygiene:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify public snapshot
        run: scripts/verify_public_snapshot.sh .
