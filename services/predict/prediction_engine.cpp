/**
 * StrayLight Prediction Engine — Implementation.
 *
 * The PredictionEngine is header-only. This translation unit provides
 * an explicit compilation point.
 */

#include "prediction_engine.h"

namespace straylight::predict {

static_assert(sizeof(PredictionEngine) > 0, "PredictionEngine must be a complete type");

} // namespace straylight::predict
