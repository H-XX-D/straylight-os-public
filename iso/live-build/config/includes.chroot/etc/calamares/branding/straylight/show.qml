// SPDX-License-Identifier: GPL-3.0-or-later

import QtQuick 2.0
import calamares.slideshow 1.0

Presentation {
    Slide {
        centeredText: qsTr("Installing StrayLight OS")
    }

    Slide {
        centeredText: qsTr("Preparing the StrayLight control-plane packages, desktop profile, and bootloader.")
    }

    Slide {
        centeredText: qsTr("Installer, firstboot, and post-install health gates are tracked separately for public releases.")
    }
}
